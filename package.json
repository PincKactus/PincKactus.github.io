{
  "name": "pinc-kactus-shop",
  "version": "1.0.0",
  "scripts": {
    "build:jekyll": "bundle exec jekyll build",
    "encrypt:secret": "npx staticrypt _site/secret/index.html _site/secret/index.html --no-open --password superSecret123",
    "build": "npm run build:jekyll",
    "build:prod": "npm run build:jekyll && npm run encrypt:secret",
    "start": "netlify dev"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "faunadb": "^4.0.0"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
